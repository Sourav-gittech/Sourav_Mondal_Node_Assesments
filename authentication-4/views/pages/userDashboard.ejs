<%- include("./../layout/header") %>

    <%- include("./../layout/navbar") %>

        <div class="container mt-5 d-flex justify-content-center">
            <div class="card bg-black text-light p-4 shadow" style="width: 400px;">
                <h4 class="text-success mb-3 text-center">My Profile</h4>

                <p class="text-dark"><strong>Name:</strong>
                    <%= user.name %>
                </p>
                <p class="text-dark"><strong>Email:</strong>
                    <%= user.email %>
                </p>

                <div class="alert alert-secondary mt-3">
                    Profile details are read-only
                </div>
            </div>
        </div>

        <%- include("./../layout/footer") %>